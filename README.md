# calculate-power-of-two
calculate a power of two

## installation
```bash
npm install calculate-power-of-two
```

## usage
```js
var twoToThePowerOf = require("calculate-power-of-two")

console.log(twoToThePowerOf(8)) // 256
```